spring:
  application:
    name: data
  profiles:
    active: development
  cloud:
    config:
      uri: http://localhost:8888
  rabbitmq:
        addresses: amqp://127.0.0.1:5672
        username: alan
        password: alan

encrypt:
  failOnError: false