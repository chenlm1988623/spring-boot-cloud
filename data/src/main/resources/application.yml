neo4j:
  datasource:
    url: http://localhost:7474
    username: neo4j
    password: 123456

server:
  port: 9000

eureka:
  client:
    serviceUrl:
      defaultZone: http://localhost:8761/eureka/
  instance:
    preferIpAddress: true

ribbon:
  eureka:
    enabled: true
